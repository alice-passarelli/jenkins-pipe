pipeline {
  agent any
  
  stages {
    stage('build') {
      steps {
        sh 'echo "Ola Jenkins"'
        sh '''
           echo "Nossa primeira pipeline"
           ls -la
           '''
      }
    }
  }
}
